name: 'CPUのビルド(Native)'

on:
  workflow_dispatch:
  push:
    paths:
      - 'buffer/cpu/cpp/**'
      - '.github/workflows/build-cpu-native-lib.yml'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS
          - os: macos-latest
            os_name: macos
            arch: Arm64
            lib_extension: a
          - os: macos-15
            os_name: macos
            arch: X64
            lib_extension: a
          # Linux
          - os: ubuntu-latest
            os_name: linux
            arch: X64
            lib_extension: a
          - os: ubuntu-24.04-arm
            os_name: linux
            arch: Arm64
            lib_extension: a
          # Windows
          - os: windows-latest
            os_name: mingw
            arch: X64
            lib_extension: a

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.os_name }}/${{ matrix.arch }})

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: Javaのセットアップ
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
          cache: 'gradle'

      - name: Fortranのセットアップ (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gfortran

      - name: MSYS2のセットアップ
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-cmake
            mingw-w64-ucrt-x86_64-ninja

      - name: gradlewに権限を付与 (Unix)
        if: runner.os != 'Windows'
        run: chmod 777 ./gradlew

      - name: OpenBLASのキャッシュを復元
        uses: actions/cache@v4
        with:
          path: buffer/cpu/cpp/build/cpu
          key: cpu-${{ hashFiles('buffer/cpu/cpp/CMakeLists.txt') }}-${{ matrix.os_name }}-${{ matrix.arch }}

      - name: ネイティブライブラリをビルド(Windows以外)
        if: runner.os != 'Windows'
        shell: bash
        run: ./gradlew :buffer:cpu:cpp:cmakeNativeBuild

      - name: ネイティブライブラリをビルド(Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: ./gradlew :buffer:cpu:cpp:cmakeNativeBuild

      - name: ログを確認
        if: failure()
        run:
          cat D:/a/knist/knist/buffer/cpu/cpp/build/openblas/src/OpenBLAS-stamp/OpenBLAS-build-*.log

      - name: 成果物をアップロード (Windows以外)
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: cpu-${{ matrix.os_name }}-${{ matrix.arch }}
          path: |
            buffer/cpu/cpp/build/libcpu.${{ matrix.lib_extension }}
            buffer/cpu/cpp/build/Release/libcpu.${{ matrix.lib_extension }}
          if-no-files-found: error

      - name: 成果物をアップロード (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: cpu-${{ matrix.os_name }}-${{ matrix.arch }}
          path: |
            buffer/cpu/cpp/build/cpu.${{ matrix.lib_extension }}
            buffer/cpu/cpp/build/Release/cpu.${{ matrix.lib_extension }}
          if-no-files-found: error

  commit:
    needs: build
    runs-on: ubuntu-latest
    name: Commit native libraries

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: 全成果物をダウンロード
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: アーティファクト構造を確認
        run: |
          echo "=== Artifacts structure ==="
          find artifacts -type f

      - name: 成果物をresourcesに配置
        run: |
          # macOS/Arm64
          mkdir -p buffer/cpu/src/nativeMain/resources/cpu/macosArm64
          find artifacts/cpu-macos-Arm64 -name "libcpu.a" -exec cp {} buffer/cpu/src/nativeMain/resources/cpu/macosArm64/ \;

          # macOS/X64
          mkdir -p buffer/cpu/src/nativeMain/resources/cpu/macosX64
          find artifacts/cpu-macos-X64 -name "libcpu.a" -exec cp {} buffer/cpu/src/nativeMain/resources/cpu/macosX64/ \;

          # Linux/X64
          mkdir -p buffer/cpu/src/nativeMain/resources/cpu/linuxX64
          find artifacts/cpu-linux-X64 -name "libcpu.a" -exec cp {} buffer/cpu/src/nativeMain/resources/cpu/linuxX64/ \;

          # Linux/Arm64
          mkdir -p buffer/cpu/src/nativeMain/resources/cpu/linuxArm64
          find artifacts/cpu-linux-Arm64 -name "libcpu.a" -exec cp {} buffer/cpu/src/nativeMain/resources/cpu/linuxArm64/ \;

          # Windows/X64
          mkdir -p buffer/cpu/src/nativeMain/resources/cpu/mingwX64
          find artifacts/cpu-mingw-X64 -name "libcpu.a" -exec cp {} buffer/cpu/src/nativeMain/resources/cpu/mingwX64/ \;

          echo "=== Resources structure ==="
          find buffer/cpu/src/nativeMain/resources -type f

      - name: 変更をコミット
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add buffer/cpu/src/nativeMain/resources/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "[CI] Update CPU(Native) libraries"
            git push
          fi
