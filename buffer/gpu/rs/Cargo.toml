[package]
name = "gpu"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
bytemuck = "1.25.0"
jni = "0.22.1"
pollster = "0.4.0"
wgpu = "28.0.0"

[features]
default = ["jni", "native"]
jni = []
native = []
