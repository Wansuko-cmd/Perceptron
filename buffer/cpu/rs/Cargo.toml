[package]
name = "cpu"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
jni = "0.21.1"
matrixmultiply = "0.3.10"
rayon = "1.12.0"

[features]
default = ["jni", "native"]
jni = []
native = []

[profile.release]
lto = true
codegen-units = 1
